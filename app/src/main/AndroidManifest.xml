<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.brq.agatha.investimentos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Investimento">
        <activity android:name=".ui.activity.CambioActivity"></activity>
        <activity
            android:name=".ui.activity.HomeMoedasActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!--    https://api.hgbrasil.com/finance?&fields=only_results,currencies&key=835f00d7-->

<!--    https://api.hgbrasil.com/finance/quotations?&fields=only_,results,currencies,USD&key=835f00d7-->